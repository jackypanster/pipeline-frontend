# roles.yaml — per-target-repo skill bindings (copy this into the target repo's .pipeline/)
# Each command resolves its slot to a skill NAME here. Point a slot at a better skill any time —
# the contract is unchanged. The runtime is ANY capable agent you paste the command into; the
# pipeline is framework-, machine- and LLM-agnostic — not bound to any tool, framework, agent or LLM.
# Per-stage model requirement: design/review want a frontier SOTA model; impl tolerates a capable local model.

design:  <design-system-skill>     # REQUIRED: your installed design-system generator (abstract slot — pick by surface, table below)
impl:    <autonomous-coding-skill> # REQUIRED: set to the real installed name of your runtime's autonomous think→code→check skill
review:  design                    # the local design/ui skill's screenshot-iteration mode (visual review vs frozen references)

# Choosing the design slot (per surface — non-binding). The four frontend quality axes
# (visual character / anti-slop / a11y / motion) are NOT all won by one generator, so three of them
# are guaranteed by the frozen DESIGN.md template (fp-design writes mandatory §Anti-slop +
# §Accessibility + §Motion discipline sections regardless of which skill fills this slot). The slot
# therefore only picks the AESTHETIC DIRECTION:
#   dashboard / back-office / data-dense → a11y-first or low-variance generator  ← the COMMON case
#     (this pipeline mostly builds API/CLI/backend frontends), the WORKING DEFAULT. taste-skill-class
#     generators self-de-scope dashboards/data-tables — don't default to them here.
#   landing / portfolio / marketing      → high-variance anti-slop or visual-adventure generator + a
#     cross-page-consistency constraint (adventure drifts across pages)  ← the RARE case here.
# Targets are desktop-primary but RESPONSIVE (adapt to tablet/phone); the generator must handle
# responsive data-dense layouts. Never bind a brand name here — abstract slot (CONTRACT §Self-improvement / DESIGN.md).

# Extend with new verbs as needed, e.g.:
# task:  think                     # decompose a large design into atomic impl cards (deferred — MVP freezes one unit per feature)
# hunt:  hunt                      # root-cause a blocked card (deferred until a real impl deadlock appears)
